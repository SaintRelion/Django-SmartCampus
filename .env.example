# Django
SECRET_KEY=
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smartcampus
DB_USER=smartcampus
DB_PASSWORD=change_me

# Email / OTP
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# WebAuthn / passkeys
RP_ID=localhost
RP_NAME=Smart Campus
ORIGIN=http://localhost:8080

# Frontend build-time API base URL
VITE_API_URL=http://localhost:8000/

# Build credentials are intentionally NOT stored here:
# SR_REACT_GITHUB_TOKEN
# SR_DJANGO_LEGACY_TOKEN
